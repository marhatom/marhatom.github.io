domain: marhatom.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://marhatom.github.io  # the base hostname and protocol for your site, e.g. http://example.com
#baseurl: /REPOSITORY-NAME/      # place folder name if the site is served in a subfolder
#title: "Home"
markdown: kramdown
kramdown:
  math_engine: mathjax
  syntax_highlighter_opts:
    block:
      line_numbers: true

theme: minima